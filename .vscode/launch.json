{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Fastify Server",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/apps/server/server.js",
      "cwd": "${workspaceFolder}/apps/server"
    }
  ]
}
